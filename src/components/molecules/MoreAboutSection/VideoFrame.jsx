import VideoBlockFrame from "@/components/atoms/MoreAboutSection/VideoBlockFrame";

export default function VideoFrame({ src, className = "" }) {
  return <VideoBlockFrame src={src} className={className} />;
}